[project]
name = "flyin"
version = "1.2"
requires-python = ">=3.10"
dependencies = [
	"pydantic",
	"pygame",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
]

[tool.mypy]
strict = true

[[tool.mypy.overrides]]
module = ["pygame.*", "pygame"]
ignore_missing_imports = true
